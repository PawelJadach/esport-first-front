import React from "react";

import { Container } from "@components";

const Home: React.FC = () => {
    return (
        <Container>
            <span>Działa</span>
        </Container>
    );
};

export default Home;
